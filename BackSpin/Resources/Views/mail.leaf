<!DOCTYPE html>
<html>
  <head>
    <title>Reservation</title>
  </head>
<body style="background-color:#0B296F;">
  
<?php
include_once "PHPMailer-master/src/PHPMailer.php";
include_once 'function.php';


if (checkParameters('email')){

  $email = $_POST['email'];


$messaggio = new PHPMailer\PHPMailer\PHPMailer();

$messaggio.IsHTML(true);

$messaggio.From = $sender;
$messaggio.AddAddress($email);
$messaggio.AddReplyTo("napples.developers@gmail.com");
$messaggio.Subject='W la figa';

$messaggio.Body = '<html><head><style>';
$messaggio.Body = 'p {color:#444444;text-align:left;font-size:15px}';
$messaggio.Body = 'span.evidenziatore {background-color:#FF0000;color:#000000;}';
$messaggio.Body = '</style></head><body>';
$messaggio.Body = '<center><p><span class="evidenziatore">Trapanarell trapanatur</span>, Trapan a nott e juorn pur</p></center>';
$messaggio.Body = '</body></html>';

if(!$messaggio.Send()){
  echo $messaggio.ErrorInfo;
}else{
  echo 'Email inviata correttamente!';
}
unset($messaggio);
}
?>
</body>
</html>
